#include <stdio.h>  // preprocessor directives
#include <math.h>
#define KMS_PERMILESS 1.609   // constant declaration

int main() {         // main function, mandatory
  float miles;
  float kms;   
  printf("miles: %lf", miles + 1);
   
  return 0;
  // changes
}


/*
  von Neumann architecture:
  desribe a computer consists of:
    Central Processsing Unit:
      Registers: expensive, few memory holding tmp results
      A control unit containing an instruction register and program counter;
      An arithmetic/logic unit;
    Memory
      Random access memory storing both programs and data
    I/O devices;
*/ 

/* Variables in C:
    Stored in 
    Contains name, data type and value
    Must declare before use;
    May be initialised, otherwise defaults on 'unknown'
*/

